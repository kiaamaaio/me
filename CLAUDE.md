# CLAUDE.md

## 著作権表示

フッターの著作権表示は [src/pages/index.astro](src/pages/index.astro) に手書きで固定してある。

```
© 2025–2026 kiaamaaio
```

年号はカレンダーではなく**コンテンツの更新**に連動する。年が変わっただけで更新する必要はなく、
更新の無い年は据え置きのままが正しい。自動生成（`new Date().getFullYear()`）はしない
— 静的ビルドのため、内容を変えていない年でもビルドしただけで年が進み、事実と食い違う。

`src/` または `public/` を変更したときは、それがサイトの**実質的な内容の更新**
（リンクの追加・削除、プロフィールや説明文の変更、デザインの変更など）かを判断すること。

- 実質的な内容の更新にあたる場合 → 末尾の年を当年に更新し、同じコミットに含める
- ビルド設定・Docker・依存関係の更新など、表示される内容が変わらない変更 → 更新しない
- 判断がつかない場合 → ユーザーに確認する

`.claude/hooks/copyright-year-check.py` が `PostToolUse` で同じ確認を機械的に行い、
末尾の年が当年を含まない場合に警告する。ただしパスでしか判断できないため、
警告が出ても上記の基準で改めて判断すること。

[LICENSE](LICENSE) の年は**更新しない**。著作権が発生した年を指すものなので、初出年のまま据え置く。
